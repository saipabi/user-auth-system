# Railway deployment configuration
[phases.setup]
nixPkgs = ["php80", "composer"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader"
]

[phases.build]
cmds = [
  "echo 'Build complete'"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
